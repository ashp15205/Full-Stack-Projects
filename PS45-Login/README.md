# PS45 - Login Form Validation
**Problem:** Login form validating email format and password minimum length with real-time error messages.  
**Stack:** HTML5, CSS3, JavaScript  
**Key concept:** Regex `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` for email + `pw.length >= 6` for password. `e.preventDefault()` blocks submission on failure.
