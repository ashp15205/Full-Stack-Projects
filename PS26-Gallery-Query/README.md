# PS26 - URL Query Filter Gallery
**Problem:** Category filter updates `?cat=shoes` in the URL and filters the gallery.  
**Stack:** HTML, CSS, JavaScript, URLSearchParams API  
**Key concept:** `history.pushState()` + `URLSearchParams` to sync URL and UI state.
