# PS33 - Timetable
**Problem:** Weekly class timetable using HTML table with `rowspan` and `colspan`.  
**Stack:** HTML5, CSS  
**Key concept:** `colspan="2"` spans multiple columns, `rowspan="2"` spans multiple rows in HTML tables.
