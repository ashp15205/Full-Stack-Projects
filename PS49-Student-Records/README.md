# PS49 - Student Records (localStorage)
**Problem:** Store and retrieve student Name + Roll No using localStorage.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `JSON.stringify()` array into localStorage, `JSON.parse()` on read, `array.splice(i,1)` to delete by index.
