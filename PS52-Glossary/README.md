# PS52 - HTML Glossary
**Problem:** Searchable table of HTML text formatting tags using DOM manipulation.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `array.filter()` on tag name + description driven by `oninput` event. `innerHTML` re-renders matching table rows.
