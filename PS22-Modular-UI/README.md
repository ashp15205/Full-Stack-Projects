# PS22 - Atomic Component Library
**Problem:** Modular UI components (Button, Input, Card) assembled as a landing page.  
**Stack:** HTML, CSS  
**Key concept:** CSS class variants (`.primary`, `.danger`, `.outline`) applied to shared base element styles.
