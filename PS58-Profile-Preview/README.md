# PS58 - Real-Time Profile Previewer
**Problem:** Profile previewer that updates as the user types including avatar initial.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `oninput` on every field → update matching preview element. Avatar initial from `name[0].toUpperCase()`.
