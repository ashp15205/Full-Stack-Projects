# PS68 - Image Gallery with Category Filter
**Problem:** Image gallery with category-based filtering.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** Data array with `cat` property. `.filter()` on category then `.map()` to build `<img>` cards. CSS `scale` on hover for zoom effect.
