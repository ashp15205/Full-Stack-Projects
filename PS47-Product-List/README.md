# PS47 - Product Filter
**Problem:** 5-item product list filtered by category using JavaScript.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `array.filter(p => p.cat === cat)` returns subset → `.map()` + `innerHTML` re-renders the grid.
