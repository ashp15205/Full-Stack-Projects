# PS44 - Dark/Light Mode Toggle
**Problem:** Dark/light mode toggle using CSS custom properties and localStorage.  
**Stack:** HTML, CSS Variables, JavaScript  
**Key concept:** CSS `--bg / --text` variables defined in `:root`. Body `.light` class overrides them. `localStorage` persists across reloads.
