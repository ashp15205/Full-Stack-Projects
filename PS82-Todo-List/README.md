# PS83 - Simple Todo List
**Problem:** To-Do List that adds and deletes tasks dynamically.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `createElement("li")` with `innerHTML` including inline delete handler via `this.parentElement.remove()`.
