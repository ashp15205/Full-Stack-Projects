# PS34 - Blog Layout
**Problem:** Blog page with header, sidebar, content area, and footer using CSS layout techniques.  
**Stack:** HTML5, CSS (Flexbox)  
**Key concept:** `display: flex` on `.layout` with `flex: 2` for main, `flex: 1` for sidebar creates the 2:1 split.
