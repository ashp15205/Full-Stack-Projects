# PS61 - Pricing Card Toggle
**Problem:** Pricing cards with monthly/yearly toggle switch.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** CSS-only toggle switch (checkbox + slider). Boolean `yr` drives which price to display in `render()`.
