# PS82 - Image Gallery with Click Preview
**Problem:** Image gallery in grid layout with modal preview on click.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** Grid thumbnails. Click changes modal `img.src` and toggles `.open` class. Modal uses `position: fixed; inset: 0` to overlay the entire page.
