# PS64 - Product Page with Quantity Controls
**Problem:** Product page with +/- qty buttons that updates total price in real-time.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** `qty[]` array tracks each item. `reduce()` sums totals. `render()` rebuilds UI on every interaction.
