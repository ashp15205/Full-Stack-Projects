# PS81 - FAQ Accordion
**Problem:** Expandable FAQ section.  
**Stack:** HTML, CSS, JavaScript  
**Key concept:** Toggle `.open` class on item. CSS `max-height: 0 → 200px` with `transition` creates smooth expand/collapse. Arrow rotates with CSS `transform: rotate(180deg)`.
